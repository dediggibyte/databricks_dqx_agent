# Dev Environment Variables
# Variables are scoped to the dev target

targets:
  dev:
    variables:
      # Application Configuration
      app_name: "dqx-rule-generator-dev"
      app_description: "DQX Data Quality Manager - Dev Environment"

      # Generation Job Configuration
      job_name: "DQ Rule Generation - Dev"
      notebook_path: "/Workspace/Users/<your-email>/databricks_dqx_agent/notebooks/generate_dq_rules_fast"

      # Validation Job Configuration
      validation_job_name: "DQ Rule Validation - Dev"
      validation_notebook_path: "/Workspace/Users/<your-email>/databricks_dqx_agent/notebooks/validate_dq_rules"

      # Note: sql_warehouse_id is passed via --var from CI/CD secrets
      # App environment variables (LAKEBASE_*, MODEL_SERVING_ENDPOINT, SAMPLE_DATA_LIMIT)
      # are configured in app.yaml via CI/CD, not as DAB variables
